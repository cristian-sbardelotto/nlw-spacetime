/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['avatars.githubusercontent.com', '192.168.0.125', 'localhost'],
  },
};

// eslint-disable-next-line no-undef
module.exports = nextConfig;
